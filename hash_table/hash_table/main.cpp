#include "hash_table.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>





int main() {


	

	//int table_size = 100007;

	//hash_table* table = init_table(table_size, MAX_INPUT_LENGTH - 2);
	//char* command = (char*)malloc(MAX_INPUT_LENGTH * sizeof(char));
	//int val;
	//if (!command) {
	//	perror("not enough memory");
	//	exit(1);
	//}


	//while (fgets(command, MAX_INPUT_LENGTH, stdin)) {
	//	command[strlen(command) - 1] = '\0';

	//	if (command[0] == 'a')
	//		insert_str(table, command + 2, MAX_INPUT_LENGTH - 3);
	//	//insert_str(table, command + 2, strlen(command - 3));
	//	else if (command[0] == 'r') {
	//		remove(table, command + 2);
	//	}
	//	else if (command[0] == 'f') {
	//		if (find_node(table, command + 2))
	//			printf("yes\n");
	//		else
	//			printf("no\n");
	//	}
	//	else if (command[0] == 'p')
	//		print_table(table);
	//}


	//free_table(table);
	//free(command);

	return 0;
}